const products = require('./products.model');
const sales = require('./sales.model');
const salesProducts = require('./sales-products.model');

module.exports = {
  products,
  sales,
  salesProducts,
};
